# 동영상 업로드 실전 가이드 (검증: 2026-07 macOS 운영분)

⚠️ **동영상이 포함된 포스팅을 하기 전에 이 문서를 반드시 끝까지 읽을 것.**
이 절차는 실제 운영 맥에서 여러 차례 발행하며 검증된 것이고, 아래 "자주 터지는 실수"는 전부 실제 사고 사례다.

## 전체 흐름 (Claude가 따라야 할 순서)

```
1. inbox의 mov/mp4 확인 → 썸네일 뽑아 내용 파악
2. 촬영 타임스탬프로 사진과 순서 정렬
3. 10개 이하로 선별 (중복 컷 제외, 제외 목록 사용자 보고)
4. 의미 있는 한글 이름으로 리네임 ← 가장 자주 빼먹는 단계
5. post.json에 video_index 배치
6. publish.py 실행 → 로그에서 "🎬 동영상 첨부 완료" 개수 검수
7. 실패분은 사용자에게 알리고 수동 첨부 안내
```

## 1. 내용 파악 (썸네일 추출)

영상 내용을 안 보고 배치하면 본문과 안 맞는 위치에 들어간다. 반드시 썸네일로 확인:

- **macOS**: `qlmanage -t -s 400 -o <출력폴더> <파일>`
- **Windows**: qlmanage가 없다. ffmpeg 사용:
  ```
  ffmpeg -i 영상.mp4 -ss 00:00:01 -frames:v 1 -vf scale=400:-1 썸네일.jpg
  ```
  ffmpeg이 없으면 `winget install ffmpeg`. 그것도 안 되면 사용자에게 영상 내용을 물어볼 것 (추측 배치 금지).
- 긴 영상은 중간 지점도 한 장 더 뽑아서 확인 (`-ss`를 영상 중간 시각으로).

## 2. 선별 규칙

- **글당 10개 이하** (네이버 제한). 초과분은 draft 폴더에 남겨두고 어떤 걸 뺐는지 사용자에게 보고.
- 같은 장면 중복 컷은 화질/길이 좋은 쪽 하나만.
- 세로/가로 혼재해도 무방. 파일 크기가 크면 업로드 대기가 수 분까지 걸리니 그대로 진행하면 된다 (스크립트가 8분까지 기다림).

## 3. 파일명 = 네이버 동영상 제목 (핵심!)

**파일명(확장자 제외)이 그대로 네이버 동영상 제목(최대 40자)으로 들어간다.**

- ❌ `IMG_4821.MOV`, `KakaoTalk_20260803_1.mp4` — 제목이 쓰레기값이 됨
- ✅ `01_가게입구-웨이팅줄.mov`, `02_거북이빵-반죽하는모습.mov`
- 숫자 접두어(`01_`, `02_`)로 삽입 순서를 유지하고, 내용 요약을 한글로. 검색 노출에도 도움됨.
- 리네임은 draft 폴더로 옮길 때 한다 (inbox 원본명은 타임스탬프 정렬에 쓰이므로 정렬을 먼저 끝낼 것).

## 4. post.json 배치

```json
{"text": "반죽하는 모습을 영상으로 담았어요", "image_index": 2, "video_index": 0}
```

- `video_index`는 `videos` 배열 기준 0부터.
- 한 섹션에 image_index와 video_index를 둘 다 주면 **사진 → 동영상 순**으로 들어간다 (스크립트가 사이에 5초 안정화 대기를 자동으로 넣음).
- 영상 소개 문단 뒤에 붙이는 게 자연스럽다 ("~을 영상으로 담아봤어요" 다음).

## 5. 발행 중 절대 하지 말 것

- **업로드 진행 중에 브라우저 창을 클릭하거나 조작하지 말 것.** 팝업이 꼬이면 이후 본문 텍스트가 전부 유실되는 사고가 난다 (실제 사례 — 스크립트가 팝업 잔재를 자동 정리하지만, 사람이 중간에 만지면 복구 불가).
- 업로드/인코딩 대기는 파일당 최대 8분. 멈춘 게 아니니 기다릴 것.
- PC 잠자기는 스크립트가 자동 방지한다 (macOS: caffeinate / Windows: SetThreadExecutionState).

## 6. 발행 후 검수 (Claude 필수 절차)

로그에서 반드시 확인:

- `🎬 동영상 첨부 완료: <파일명>` 줄 수가 배치한 영상 수와 일치하는가
- `⚠️ 동영상 팝업 열기 실패` / `⚠️ 업로드 완료 대기 시간 초과` / `⚠️ 동영상 자동첨부 실패`가 있으면 → 해당 영상은 **빠진 채 발행 직전 상태**다. 사용자에게 알리고, draft 폴더의 파일을 에디터에서 수동 첨부하도록 안내 (실패해도 파일은 draft에 남아 있음).
- 실패가 반복되면 publisher.py의 `_insert_video` 셀렉터를 네이버 UI 변경에 맞춰 수정해도 된다 (수정 내용은 사용자에게 보고). 현재 셀렉터 흐름:
  `button[data-name="video"]` (툴바) → `.se-popup-video-upload` (팝업) → `button.nvu_btn_local`/`nvu_btn_append` (파일선택) → `.nvu_btn_select`에 "업로드 완료" 텍스트 대기 → `input.nvu_inp` 제목 입력 → `button.nvu_btn_submit`.
  ⚠️ 툴바 버튼에도 "동영상 추가" 텍스트가 있으므로 텍스트 매칭은 반드시 팝업 범위로 한정할 것.

## 자주 터지는 실수 체크리스트

| 실수 | 결과 | 예방 |
|---|---|---|
| 리네임 안 함 | 동영상 제목이 IMG_1234 | 3번 규칙 |
| 썸네일 확인 없이 배치 | 본문과 영상 내용 불일치 | 1번 규칙 |
| 11개 이상 배치 | 업로드 실패/누락 | 10개 선별 |
| 업로드 중 창 조작 | 본문 유실 | 5번 규칙 |
| 로그 검수 생략 | 영상 빠진 채 발행 | 6번 규칙 |
| Windows에서 qlmanage 시도 | 에러 | ffmpeg 사용 |
