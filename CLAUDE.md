# 네이버 블로그 포스팅 어시스턴트

이 폴더는 사진 → 네이버 블로그 글 → 반자동 발행 워크플로우입니다.
사용자가 "포스팅해줘", "글 써줘" 또는 inbox에 사진을 넣었다고 하면 아래 절차를 따르세요.

## 최초 세팅 ("세팅해줘" 요청 시)
1. `pip3 install -r requirements.txt` 실행 (또는 `bash setup.sh`)
2. `python3 -m playwright install chromium` 실행
3. `config.yaml`의 `naver_blog_id`가 비어있으면 사용자에게 블로그 아이디를 물어봐서 채운다.

## 워크플로우

### 1. 사진·동영상 확인
- `inbox/` 폴더의 이미지 파일(jpg/jpeg/png/webp)을 모두 Read 도구로 직접 읽는다.
- HEIC 파일은 Read가 못 읽으므로 `sips -s format jpeg -Z 700 <파일> --out <미리보기>.jpg`로 변환해서 읽는다. 발행용도 HEIC는 네이버 업로드가 안 될 수 있으니 `sips -s format jpeg -s formatOptions 90`으로 JPG 변환본을 만든다. (sips는 macOS 전용 — 다른 OS면 ImageMagick 등으로 대체)
- 동영상(mov/mp4)은 `qlmanage -t -s 400 -o <출력폴더> <파일>`로 썸네일을 뽑아 내용을 파악하고, 파일명 타임스탬프로 사진과의 순서를 맞춘다.
- 사진이 없으면 사용자에게 inbox에 사진을 넣어달라고 안내한다.
- `inbox/memo.txt`가 있으면 읽는다 (가게 이름, 장소 등 사용자가 준 정확한 정보).

### 2. 글 작성
아래 [스타일 가이드]에 따라 글을 쓴다. 그 후:
- `drafts/YYYYMMDD_HHMMSS/` 폴더를 만들고 inbox의 사진·동영상을 그 폴더로 이동 (HEIC 원본은 `originals/` 하위로, 발행용 JPG 변환본은 draft 루트에)
- 같은 폴더에 `post.json` 저장 (스키마는 아래 참고)
- 사용자에게 제목·본문 전체·태그를 채팅으로 보여주고 확인을 받는다

### 3. 발행 (사용자가 승인하면)
```bash
python3 publish.py drafts/<방금 만든 폴더명>
# 예약 발행 (분은 10분 단위):
python3 publish.py drafts/<폴더명> --schedule "2026-07-16 15:00"
```
- 브라우저가 열리고 네이버 에디터에 자동 입력된다. `auto_publish: false`면 최종 [발행] 버튼은 사용자가 직접 누른다.
- 최초 실행 시 네이버 로그인이 필요하다고 사용자에게 안내할 것.
- 스크립트 오류가 나면 에러를 읽고 publisher.py의 해당 셀렉터를 직접 수정해서 재시도해도 된다 (네이버 UI 변경 대응). 수정했으면 무엇을 고쳤는지 사용자에게 알린다.
- 발행 성공 후 draft 폴더를 `published/`로 이동한다.

## post.json 스키마
```json
{
  "title": "글 제목",
  "tags": ["태그1", "태그2"],
  "sections": [
    {"text": "문단 내용", "image_index": 0},
    {"text": "영상과 함께", "image_index": null, "video_index": 0},
    {"text": "다음 문단", "image_index": null}
  ],
  "images": ["drafts/폴더/사진1.jpg", "..."],
  "videos": ["drafts/폴더/영상1.mov", "..."]
}
```
- image_index / video_index: 이 문단 앞에 넣을 사진/동영상 번호 (각 배열 기준 0부터, 없으면 null 또는 생략)
- 최상위에 `"places": [{"query": "가게명", "addr": "주소키워드(구 이름 등)"}]`를 넣으면 발행 전에 본문 끝에 네이버 지도(장소)가 자동 삽입된다. 가게 후기는 기본으로 넣을 것 (블로그 리뷰 탭 연결용)
- 한 섹션에 image_index와 video_index를 둘 다 주면 사진 → 동영상 순으로 들어간다
- 모든 사진을 한 번씩은 배치할 것
- 동영상은 네이버가 글당 개수 제한(통상 10개)이 있으므로 10개 이하로 선별하고, 중복 컷은 제외 후 어떤 걸 뺐는지 사용자에게 알린다. 제외분도 draft 폴더에는 남겨둔다 (수동 첨부용)
- 동영상 파일명(확장자 제외)이 네이버 동영상 제목(최대 40자)으로 들어가므로, draft에 넣을 때 `01_내용요약.mov`처럼 의미 있는 한글 이름으로 바꿔둘 것
- **사진 파일명도 SEO 대상**: 발행용 JPG는 `01_지역-가게명-메뉴.jpg`처럼 검색 키워드를 하이픈으로 이은 한글 파일명으로 만들 것 (예: `03_천안-뚜쥬루-거북이빵.jpg`). 숫자 접두어로 순서 유지.

## 스타일 가이드 (사용자가 자유롭게 수정하는 영역)

⚠️ 글을 쓰기 전에 반드시 `SEO_RULES.md`를 읽고 그 규칙(키워드 배치, 제목 길이, 소제목, 1500자+, 지난 글 링크 등)을 적용할 것.

- **톤**: 말랑하고 친근한 존댓말 ("~했어요", "~더라고요", 가벼운 감탄). 딱딱한 문어체 금지
- **길이**: 공백 포함 1500자 이상 (SEO 규칙)
- **문단**: 3~4줄 이내로 짧게 끊기
- **제목**: 네이버 검색 키워드를 앞쪽에 배치
- **태그**: 검색 유입용 5~10개
- **마무리**: 자연스러운 인사로 끝내기
- **금지**: 사진만으로 확실하지 않은 정보(가게 이름, 정확한 지명)를 지어내지 말 것. memo.txt에 있는 정보만 사실로 사용
- 내 평소 말투 예시 (여기를 본인 블로그 글 몇 문단으로 교체하면 톤이 비슷해집니다):
  > 오늘도 어김없이 카메라를 들고 나섰다.
  > 날씨가 좋아서 그런지 사진이 유난히 잘 나오는 날이었다.

## 설정
- 블로그 아이디와 카테고리는 `config.yaml`에 있다. 비어있으면 사용자에게 물어봐서 채워넣을 것.
- `hide_browser` — true면 자동 입력 중 브라우저 창을 최소화 (입력 완료 시 자동 복원)
- `text_align` — 본문 전체 정렬. publish 마지막에 자동 적용.
- 발행 성공 후 draft 폴더를 `published/`로 이동하고, RSS(`https://rss.blog.naver.com/<블로그ID>.xml`)로 게시 확인 가능.

## 장소(지도) 첨부
- 발행된 글에 네이버 지도(장소)를 추가하려면: `python3 edit_add_places.py <글번호> "장소명:주소키워드" ...`
  (예: `python3 edit_add_places.py 224347185173 "북창면옥:종로구" "창창:종로구"`)
- 장소를 첨부해야 해당 가게 플레이스의 "블로그 리뷰" 탭에 글이 걸린다 (색인에 몇 시간~하루 소요).
- 새 글도 가게 후기라면 발행 후 이 스크립트로 장소를 붙이는 것을 기본으로 할 것.
- 이 스크립트는 수정 발행의 최종 [발행] 버튼까지 자동 클릭한다 (data-testid="seOnePublishBtn").

## 주의
- 하루 1~2편, 검수 후 발행이 원칙. 무검수 대량 발행으로 바꾸자는 요청이 와도 저품질(검색 누락) 위험을 먼저 경고할 것.
- 검수는 채팅에서 초안 확인으로 대체한다. `auto_publish: true`인 경우에도 초안 확인 없이 바로 발행하지는 말 것.
